# K-Rez — Reglas del proyecto

## Documentación

La documentación de esta app web (`web/`) debe mantenerse actualizada conforme cambia el código.

Antes de publicar/actualizar documentación, confirmar con el usuario. Una vez confirmado, sincronizarla a:

```
rez@100.82.19.31:/home/rez/k-rez/webapp-docs/
```

(servidor de Plan A, alcanzable solo vía Tailscale).
